# Tareas completadas 2026-09-20

## 119A-2 Fase B + B0 + delete-site + incidente VPS (20-09)
- **Qué:** B0 (E11 sin DNS omite rollback ciego: `extraer_host_salud` +
  `dominio_salud_resuelve` en `host_preflight.rs`, 2 tests); capacidad nueva
  `delete-site` (`src/commands/delete_site.rs`: confirmación tipada, ruta
  validada, `compose down --volumes` acotado, `rm -rf` validado, DELETE con
  banderas seguras, verificación 404 + 11 intactos, `remove_site`; CLI +
  dispatch + README; 2 tests); prueba remota B1/B2/B3 con `cm-test-119a2`
  (uuid `q4co88c844w0ckso88c8cc4g`, repo `task.git`, rama `deploy-pre-fase0`).
- **Evidencia:** `cargo test --lib` 184/184; `check --all-targets` EXIT 0;
  release `5m28s`; B1 `new` + A record creado y resolviendo + health
  `/api/health`; fases B2 [1/6]–[3/6] OK (compose sync, primer deploy API,
  postgres OK, build 523 s tras reintento); B3 `delete-site` 5/5 OK
  (host + API 404 + settings limpios). Incidente y recovery en
  `Agente/prevencion/prevencion-docker-restart-build-vps-2026-09-20.md`.
- **Incidente:** dockerd reiniciado 17:19:40 CEST en ventana de build
  (desencadenante desconocido; sin OOM/apt/stop manual; `NRestarts=4`);
  caída total 11 sitios. Recovery autorizado por fases: 9/11 con `docker start`,
  studio/kamples con `rm` + `compose up` acotado (volumen anónimo do8k
  rescatado con `cp -a`, imagen mo4so reconstruida). Verificación HTTP final:
  10×200 + cap 302→/cap-login/ (normal). Sin pérdida de datos; backups sin usar.
- **Decisiones:** B2-retry en VPS productiva CANCELADO; 119A-4 pasa a PRIORITARIA
  con datos del incidente. DNS `cm-test-119a2` residuo documentado (apunta a IP
  sin stack). Volumen huérfano do8k `0f19bac9…` conservado como respaldo.
- **Commits:** `0f108ac` (B0), `efdf505` (delete-site), + fix idempotencia
  DELETE-404 (este lote).
- **Gotchas:** `compose up` sigue labels (rename no oculta contenedor);
  `--force-recreate` sustituye volúmenes ANÓNIMOS (pérdida de datos);
  DELETE Coolify es encolado (verificar 404 con reintento, no inmediato);
  `setup-site-dns` duplica esquema en verify (`https://https://…`, cosmético).

## B4 Test E2E ruta clásica build-in-VPS con red de seguridad (20-09 noche)
- **Qué:** plan `Agente/planes/completados/plan-B4-test-clasico-2026-09-20.md`
  (sustituye B2-retry cancelado). Desechable `cm-test-b4` (`jgkks048…`, repo
  `task.git`, rama `deploy-pre-fase0`): P0 preflight (load 3.39, RAM disp 5.3 GB,
  dockerd baseline 17:19:40/NRestarts=4, 28 contenedores 0 Restarting, salud
  10×200+cap 302), P1 watchdog VPS (`/tmp/cm-watchdog.sh`, mata solo build si
  load≥8 ×2, 40 min), P2 `new` + `setup-site-dns` (A creado), P3
  `deploy-service --skip-backup` clásico, P4 200 + colateral, P5 `delete-site` +
  residuos + salud final, P6 cierre.
- **Evidencia:** `Deploy exitoso! …/api/health … (status=200)`; colateral manager
  11/11; salud final CLI 11/11 (`health --name` ×11); watchdog `sin-flag`;
  dockerd sin cambios. **Ruta clásica validada con binario F1–F4.**
- **Hallazgos (tareas en roadmap):** E20 falso positivo en primer deploy
  (postgres sin init → necesita espera readiness); `new --template rust` deja
  `httpPath "/"` → 404 en app sana + cascada rollback innecesaria (default debe
  ser `/api/health`); `delete-site` no retira timer autoheal; dns_manager sin
  borrado (residuo A `cm-test-b4` + `cm-test-119a2`); redeploy-API último
  recurso dio 404 Coolify (investigar qué intenta redesplegar).
- **Limpieza:** stack eliminado (API 404 verificado), settings sin entrada,
  timer/imagen/uploads/`.last`/watchdog/tmp eliminados, backup local
  `compose-backups/cm-test-b4` borrado. Sin commits de código (solo docs).

## Fix-all hallazgos B4-1..B4-5 (20-09 noche)
- **Que:** correccion de los 5 hallazgos del test B4 en `main` (13 ficheros + `delete_dns.rs` nuevo, +610/-55).
- **B4-1:** `HealthCheckConfig::rust_default()` (`/api/health`, resto de campos = default); `new --template rust` lo usa. Test `test_rust_health_default_usa_api_health`.
- **B4-2:** E20 reintenta 6x10 s (`db_existe_ahora`) antes del veredicto; mensaje final conserva lista de BDs. Sin test unitario (requiere SSH); veredicto final identico.
- **B4-3:** `delete-site` retira timer+service+script autoheal, imagen `<uuid>-app` y uploads vacios (`rmdir`) con marcador `DELETE_SITE_RESTOS_OK`, best-effort. Tests `unidad_autoheal_misma_regla_que_instalador`, `comando_restos_cubre_timer_imagen_uploads`.
- **B4-4:** `delete_site_dns` + comando `delete-dns` (confirmacion tipada FQDN, `--confirm` solo sin dry-run); regla solo-si-apunta-a-VPS, `kept-remote`/`ambiguous-skipped`/`absent`. Metodos `delete_dns_record` (CF) y `delete_dns_zone_record` (Contabo). `delete-site` pasa a 6 fases con DNS [5/6] best-effort. Test `test_plan_borrado_solo_si_apunta_a_la_vps`. Verificado dry-run real: `A cm-test-b4` y `A cm-test-119a2` -> `would-delete`. Borrado real pendiente de autorizacion del usuario (escritura remota).
- **B4-5:** endpoint oficial `POST /api/v1/deploy` + `{"uuid"}` verificado contra https://coolify.io/docs/api/endpoints/deployments/deploy-by-tag-or-uuid (citado en codigo). Test `deploy_usa_endpoint_oficial` ya existia.
- **Evidencia:** `cargo check --all-targets` OK; `cargo test --lib` 196/196; `cargo build --release` OK; binario release `delete-dns --dry-run` contra Cloudflare real. Clippy: 0 errores en codigo nuevo; 16 preexistentes por toolchain 1.95 en ficheros no tocados (tarea separada en roadmap).
- **Leccion herramienta:** el patron "edit de ancla que termina en `{`" destruye la llave si `newString` no la repone; a partir de ahora, anexar tests en UN solo edit con contexto completo.
