# Tareas completadas — 2026-09-09

## db-compare-v2 + veredictos WordPress vs legacy 13/08 (Fase 3+4, sin restore)

- **Qué:** fix del falso negativo de `db-compare` (veredicto VERDE/AMARILLO/ROJO/GRIS,
  baseline fijada `--dump legacy:<sufijo>`/`vps:`, fecha del dump, `SinReferencia`,
  aviso fail-closed sin baseline, soporte legacy `.tar.gz` read-only) + veredicto 100%
  por WP vivo vs legacy 13/08.
- **Resultado:** 5/5 AMARILLO benigno, ningún ROJO/GRIS, ningún restore procede.
  guillermo 10/12 (críticas users/posts intactas), padel 25/27 (+16 spam casino),
  wandori 12/14 (+4 spam, sonda `oast.me` inerte), nakomi 28/39 (app activa,
  tareas 242=242, 0 pérdidas de negocio), cap 19/21 (+1 spam). Divergencias = spam
  post-13/08 + `wp_options` volátiles (`cron`/`transients`) + updates legítimos.
- **Archivos:** `src/services/compare/report.rs`, `src/infra/db_tmp.rs`,
  `src/services/compare_manager.rs`, `src/commands/db_compare.rs`.
- **Evidencia/tests:** `cargo test --lib` 180 passed; 15 tests `compare`; clippy 0
  errores (13 warnings pre-existentes); fmt limpio en hunks propios; build release OK;
  JSON en `C:\tmp\dbcompare-{guillermo,padel,wandori,nakomi,cap}-legacy.json` (temporal).
- **Docs:** diagnóstico §8 (veredictos) + §10 (conclusión final, gitignored),
  incidente §0.3, `roadmap.md` (E12-v2 + pendientes 5-6 nuevos: backup archivos WP,
  caso cuenta guillermo), plan movido a `Agente/planes/completados/`.
- **Gotchas:** sufijo legacy varía por sitio; `host-exec` sin `--name`/`--timeout`
  (usar `timeout N` dentro); shim cargo bloqueado → cargo real 1.95.0; `C:\tmp` llegó
  a 0 free → borrado `debug/` propio (~4 GB).
- **Commits:** `732add2` (Fase 3) + `1cbefd2` (Fase 4) + cierre documental.
- **Pendientes reales:** push con autorización; backup archivos WP post-13/08 sin copia;
  identificar cuenta cliente guillermo + reset password (autorización + snapshot).
