# Tareas completadas — 2026-09-16 (coolify-manager-rs)

## Op sitio `guillermo` (guillechatbots.es): fotos Sobre Mí + planes sin precio

**Origen:** pedido directo del usuario (sin ID roadmap; operación sobre sitio cliente, no código del repo).
**Snapshot previo:** `mariadb-owck8sww4ogk8gskgwcsk4w0/daily/2026-09-16_1220.sql.gz` (dump previo a cambios).

**Qué se hizo (todo vía `coolify-manager.exe`, sin SSH directo):**
- Imágenes `C:\Users\Owner\Downloads\1.png` (1086×1448) y `2.png` (1536×1024): el `fill` de
  `<input type=file>` falla en el navegador beta, así que se transfirieron troceadas en base64
  (107+104 trozos de 24 KB; 64 KB rompe el límite de línea de comandos de Windows) con
  verificación MD5 (`a6280ce0…`, `02876bd9…`).
- Import a Medios vía `media_handle_sideload` (GD/imagick OK): IDs 47/48,
  `.../uploads/2026/04/guillermo-perfil.png` y `.../guillermo-trabajando.png` (ambas HTTP 200).
- Opciones `glory_opcion_glory_image_hero` / `glory_opcion_glory_image_secondary` actualizadas
  (solo valor principal; el frontend las lee vía `OpcionRepository::get` → `window.__GLORY_CONTENT__`).
- Precios: vaciadas `glory_opcion_glory_pricing_{basico,avanzado,total,currency,period}` (valores
  anteriores 99/149/199/€ viven como default en `App/Config/opcionesTema.php`) + CSS en Customizer
  `#pricing-grid .flex.items-baseline.gap-1.mb-2{display:none;}` porque `PricingCard.tsx` renderiza
  el precio incondicionalmente. Sin rebuild del bundle React (fuente+dist en VPS, sin node local).
- Limpieza `/tmp` del VPS (`up*.b64`, `up*.png`, scripts `who/reset/probe/img/pr.php`).

**Evidencia/verificación (navegador beta, DOM real):**
- `/sobre-mi/`: `guillermo-perfil.png` `naturalWidth=1086 complete=true`;
  `guillermo-trabajando.png` decodificada `naturalWidth=1536` (el lazy nativo no dispara solo en el
  renderer beta; con `loading=eager` carga; sirve HTTP 200 con bytes íntegros).
- `/planes/`: las 3 filas de precio en `#pricing-grid` con `computed display=none`, sin texto de
  precio en el grid; las 3 tarjetas (Básico/Avanzado/Total) siguen presentes.
- Limitaciones beta reportadas al usuario: `snapshot` vacío, `screenshot` → `UnknownVizError`,
  `evaluate` solo una expresión, `fill` no funciona en file inputs.

**Reversión:** re-entrar precios en `/configuracion` → Precios (o defaults del código) + borrar el
bloque `/* Guillermo 2026-09-16 */` del CSS adicional; imágenes anteriores: defaults
`default-hero-*.webp` del tema. No hay commit asociado (cambios solo en VPS/BD).
