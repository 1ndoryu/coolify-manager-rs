# Tareas completadas — 2026-09-11 (coolify-manager-rs)

## 119A-1: Extracción mecánica de `deploy_service.rs` a submódulos (F1/F2 del plan monolito)

**Origen:** `039A-1` Fase 4 · **Plan:** `Agente/planes/completados/plan-monolito-deploy-service-2026-09-10.md`
**Alcance cumplido:** extracción mecánica de los seams 1, 2, 3, 4, 6, 7, 8, 9, 10 sin cambiar
lógica, orden ni mensajes; `pub async fn execute` conserva firma y ruta (llamantes intactos).
`execute()` (seam 5/F3) NO se descompuso: sin cobertura directa y sin autorización de verificación
funcional contra Coolify real (limitación declarada en §6 del plan).

**Qué se hizo (3 bloques, commit+push por bloque):**
- Bloque A (`b87ec08`): `compose_backup`, `postgres_inspect`, `compose_validation`,
  `container_verification` (seams 1–4).
- Rig (`e055c04`): `scripts/quality/cargo-stage.ps1` (fija `CARGO_TARGET_DIR` fuera del árbol:
  `GLORY_CARGO_TARGET_DIR` o `C:\tmp\glory-target\coolify-manager`) + `scripts/quality/stages-rust.json`
  (fmt `--check`, clippy `--all-targets -- -D warnings`, test `--lib`) + `.gitignore`.
- Bloque B (`b44ed8d`): `env_building`, `postgres_auth` (seams 7–8); import de tests ajustado
  (`use super::env_building::...` dentro de `network_recovery_tests`).
- Bloque C (`b784d5a`): `compose_sync`, `host_preflight`, `rust_autoheal` (seams 6, 9, 10);
  cross-imports entre seams vía `super::` (`compose_sync`→`compose_backup`/`compose_validation`/
  `env_building`; `host_preflight`→`rust_autoheal`; `rust_autoheal`→`env_building`/`postgres_auth`).

**Archivos:** `src/commands/deploy_service.rs` 2702→842 líneas (2135→661 efectivas, algoritmo del
gate); 9 submódulos nuevos en `src/commands/deploy_service/`, todos <500 efectivas
(mayor: `compose_sync` 417, `rust_autoheal` 280).

**Evidencia/gate (`sentinel check 119A-1 --stages scripts/quality/stages-rust.json`, target en `C:\tmp`):**
- `test` PASS — 180 passed, 0 failed (sin regresión respecto a los 10 tests inline previos).
- `clippy`/`fmt` ERROR solo por hallazgos preexistentes ajenos al refactor (`db_compare`,
  `diagnose`, `new_site`, `services/compare/*`, `site_capabilities`, hunk `FRONTEND_DIR`
  movido verbatim); **cero findings en `src/commands/deploy_service*`**.
- Coordinación: `claim 119A-1` OK; `start` bloqueado por árbol sucio ajeno (WIP del usuario:
  `gui/src-tauri/src/main.rs`, `src/config/mod.rs`, borrados `google_drive`, `tailscale_manager`);
  se trabajó sin worktree, con stage explícito por archivo en cada commit. Rama `main` en sync con
  `origin/main` tras cada bloque.

**Medición final:** `limite-lineas-nivel-3` eliminado (661/500 = 1,32× → nivel-1 residual,
concentrado en `execute()`).

**Gotchas (para reutilizar):**
- `replaceAll "\nfn "` duplica visibilidad si el texto ya trae `pub(crate)` en la misma línea
  compuesta; verificar con grep `^pub(crate)` + conteo tras cada tanda.
- `use super::*;` en tests NO cubre nombres movidos a submódulos cuando el padre deja de
  importarlos (lint `unused_imports` en lib): importar explícito dentro del `mod tests`.
- `rustfmt` ordena `use super::` antes que `use crate::`, y minúsculas antes que `CamelCase`
  en el mismo grupo; los slices traen `}\r\n\r\n` final (doble newline CRLF) que hay que recortar.
- `Set-Content` sin `-NoNewline` puede alterar el newline final: comprobar bytes (`0D 0A`)
  tras cada cirugía por líneas.

**Pendiente real:** `119A-2` (F3) — descomponer `execute()`; bloqueada hasta autorización de
verificación funcional contra Coolify real. Prevención: ninguna nueva (el plan ya declara el riesgo).
