# Tareas completadas — 2026-09-22 (coolify-manager-rs)

## Op VPS: `wan@nakomi.studio` → reenvío a `andoryyu@gmail.com` (postfix + SRS)

**Origen:** pedido directo del usuario. Todo vía `coolify-manager.exe`, sin SSH directo.

**Hallazgos previos (solo lectura):**
- DNS ya apuntaba al VPS: `MX nakomi.studio → mail.nakomi.studio → 66.94.100.241`; sin TXT/SPF.
- Sin MTA instalado, sin contenedor de correo, puerto 25 cerrado (ufw sin regla, INPUT DROP).
- Salida 25 OK (handshake contra `gmail-smtp-in` verificado); PTR genérico Contabo
  `vmi3001645.contaboserver.net`; Ubuntu 24.04. Proceso `apt-get update` colgado 74 días (stale,
  solo `update`, sin transacción dpkg) → `kill` seguro y reintento OK.

**Qué se hizo:**
- `apt-get install -y postfix postsrsd` (3.8.6).
- `main.cf` (vía `postconf -e`, sin heredocs): `myhostname=mail.nakomi.studio`,
  `mydomain/myorigin=nakomi.studio`, `mydestination=localhost`,
  `virtual_alias_domains=nakomi.studio`, `virtual_alias_maps=hash:/etc/postfix/virtual`,
  `inet_protocols=ipv4`, `smtp_tls_security_level=may`, canonical SRS contra
  `127.0.0.1:10001/10002`; `/etc/default/postsrsd` con `SRS_DOMAIN=nakomi.studio` (1 línea).
- `/etc/postfix/virtual`: `wan@nakomi.studio andoryyu@gmail.com` + `postmap`.
- `ufw allow 25/tcp` (+v6); `enable --now postsrsd postfix` (ambos `active`, 25 en escucha).
- Limpieza: scripts temporales del VPS eliminados tras cada prueba.

**Evidencia (syslog):** el pipeline alias→SRS→relay funciona; Gmail acepta conexión y solo
rechaza por SPF ausente (`dsn=5.7.26 ... SPF [nakomi.studio] with ip: [66.94.100.241] = did not pass`).
Banner local `220 mail.nakomi.studio-ESMTP` verificado contra la IP pública desde el propio VPS.

**Pendiente del usuario (2 acciones externas, no ejecutables desde aquí):**
1. DNS: crear `nakomi.studio TXT "v=spf1 a:mail.nakomi.studio ~all"`.
2. Contabo: ticket pidiendo desbloquear el 25/tcp **entrante** (verificado: pila local OK con
   `curl smtp://66.94.100.241:25`, pero desde internet el SYN no llega — filtro aguas arriba).
3. Test final: enviar desde Gmail a `wan@nakomi.studio` y confirmar llegada a `andoryyu@gmail.com`
   (mirar spam la primera vez); el `status=sent` se verifica en `/var/log/syslog`.
