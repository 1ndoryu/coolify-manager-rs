# Tareas completadas — 2026-05-12

## 125A-1+125A-2 — Auth backend en gui_api.rs (JWT + Argon2 + rate limit + bootstrap admin)
- **Qué:** Creado `src/auth.rs` con: store de usuarios en memoria (`Arc<RwLock<Vec<User>>>`), hashing Argon2 para contraseñas, generación y validación de JWT (15 min de vida), rate limit por IP (5 intentos / 15 min), bootstrap admin desde env vars `ADMIN_EMAIL` + `ADMIN_PASSWORD`, endpoints `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`, middleware `auth_middleware` que protege `/api/command`.
- **Archivos:** `src/auth.rs` (nuevo), `src/lib.rs` (añadido `pub mod auth`), `src/gui_api.rs` (reescrito `run()`, añadido `build_cors()`), `Cargo.toml` (añadido `argon2 = "0.5"`)
- **Gotchas:** La función `auth_middleware` devuelve `Response` (no `Result<Response, _>`) porque Axum `from_fn_with_state` espera `impl IntoResponse`. Se usa `from_fn_with_state(auth_state, auth_middleware)` aplicado con `route_layer` solo sobre `/api/command`, dejando auth routes independientes con su propio `.with_state(auth_state)`. CORS en LOCAL_MODE usa `Any`; en modo online usa `ALLOWED_ORIGIN` env con headers explícitos.
- **Sentinel:** No aplica (auth no tiene reglas Sentinel candidatas)
- **GLORY:** No aplica (auth es específico de esta herramienta operativa)

## 125A-3 — Login wall en frontend
- **Qué:** Creados `gui/src/hooks/useAuth.ts` (estado de sesión, login/logout, token en memoria con `tokenEnMemoria` privado), `gui/src/componentes/VistaLogin.tsx` (form email+password, feedback de error, spinner), `gui/src/estilos/login.css` (usando solo variables de `variables.css`). Actualizado `clienteCoolify.ts` para exportar `apiLocalUrl` y `setAuthToken`, y adjuntar `Authorization: Bearer` a todas las llamadas. Actualizado `App.tsx` con guard `if (!auth.autenticado) return <VistaLogin />`.
- **Archivos:** `gui/src/hooks/useAuth.ts` (nuevo), `gui/src/componentes/VistaLogin.tsx` (nuevo), `gui/src/estilos/login.css` (nuevo), `gui/src/servicios/clienteCoolify.ts` (exports + header), `gui/src/App.tsx` (guard + import VistaLogin)
- **Gotchas:** El token vive en módulo-level (`tokenEnMemoria`) — se pierde al cerrar pestaña, intencional para seguridad. LOCAL_MODE: `/api/auth/me` devuelve `{email: "local@operator", local_mode: true}` sin necesitar token, por lo que el frontend se autentica automáticamente en modo local. CSS de loading (`contenedorCarga`) está en `login.css` e importado en `App.tsx`.
- **Sentinel:** No aplica
- **GLORY:** No aplica

## 105A-45 — Nakomi Studio cleanup
- **Qué:** Eliminada referencia a Contabo en `SolucionVpsIsland.tsx` (feature "Escalado claro" y SEO description). VPS card en `SolucionesIsland.tsx` actualiza `enlace` a `https://vps.nakomi.studio`. `humanize_tier_name` en `vps_stripe.rs` renombrada de "Cloud VPS N" a "Nakomi VPS N".
- **Archivos:** `frontend/src/islands/SolucionVpsIsland.tsx`, `frontend/src/islands/SolucionesIsland.tsx`, `src/services/vps_stripe.rs` (en repo `glory-rust-template`, rama `glory-rust-nakomi`)
- **Gotchas:** El `spaClick` en `SolucionesIsland.tsx` acepta links externos (href absoluto `https://...`) — no necesitó cambio. El `TarjetaSolucion` usa `href={solucion.enlace}` directo, lo cual es correcto para link externo.
- **Sentinel:** No aplica
- **GLORY:** No aplica (cambio de marca específico de Nakomi)
